# Source of truth for local secrets. Hosted builds (Vercel, Convex dashboard,
# EAS) still need their own copies unless a Doppler integration is connected.
setup:
  - project: rlpeng
    config: dev
    path: .
